# NEXUS — Demonstration Transcript

> Generated by `demo/prove-it.sh` on 2026-07-04. Reproduce it:
> `bash demo/prove-it.sh` (requires Rust; runs offline).

**The claim under test:** NEXUS memory is made of *positions that can lose* —
falsifiable at admission, settled by events, cascading on retraction, shaping
behavior, traceable to evidence, and settled *automatically* where mechanics allow.

**Why a mock model:** the AI is a deterministic stub so nothing here can be
credited to a model. Ids and timestamps vary per run; every behavior is fixed.
A real provider (Anthropic/OpenAI/Gemini/OpenRouter/Ollama/LM Studio/Hugging
Face) drops in via `nx configure` with zero code changes — knowledge belongs
to the record, not the model.

## 1. ADMISSION: a belief that cannot lose is refused storage

A conventional memory stores anything. Here, the only write path enforces the admission rule: no falsifiers, no bet.

```
$ nx bet place "I am always right" --falsifier "   "
Error: no falsifiers, no bet: a position that cannot say what would kill it may be an episode, never a reliance (specs/record.md §11.1)
```

**PASS** — unfalsifiable belief rejected, rule quoted back

**PASS** — negative control: the record holds zero beliefs afterward

## 2. SETTLEMENT: confidence is a score, not an adjective

A belief and a dependent belief are placed; the first earns score by surviving contact with events.

```
$ nx bet resolve <A> held
resolved 01KWPB5V64DABDMXAN6716D4D6 as Held
```

**PASS** — belief A: held:1 — earned, never asserted

## 3. THE LOSS: evidence kills A — the cascade flags what A justified

One resolution event falsifies A. Dependency-directed retraction marks the dependent belief B UNJUSTIFIED — a conclusion cannot silently outlive its premises.

```
$ nx bet resolve <A> falsified
resolved 01KWPB5V64DABDMXAN6716D4D6 as Falsified — 1 dependent bets marked unjustified (RECONCILE)
```

**PASS** — RECONCILE cascaded to the dependent belief

**PASS** — B carries [UNJUSTIFIED] in every future retrieval

## 4. THE LITMUS TEST: 'show me a belief you lost, and what killed it'

Ask any AI assistant this question. Then run one command here.

```
$ nx bets --lost
01KWPB5V64DABDMXAN6716D4D6  LOST (falsified)
    believed: the staging db is safe to wipe
    killed by: July wipe destroyed the analytics team's prod-mirrored dataset (by user)
```

**PASS** — answered from the ledger, with the killing evidence

## 5. BEHAVIOR: a rejection becomes memory that changes the next judgment

Task 1 runs before any failure memory exists (control). Its rejection mints a premortem. Task 2, similar, must heed exactly that one caution — an out-of-scope premortem is planted as a second control and must stay out.

**PASS** — control: task 1 plan is caution-free (no memory yet)

```
$ nx do "add a config file for logging" --mock
task 01KWPB60VN0S6QH7CGXDSDXH6Z
  plan: record the intent in NOTES.md (mock worker)
  plan: heeding 1 caution(s) from memory
  1 effects staged in txn 01KWPB60WRSD1DM7NBEHVY2N6E
  critic verdict: approve
review with `nx inbox`, then `nx approve 01KWPB60WRSD1DM7NBEHVY2N6E` or `nx reject 01KWPB60WRSD1DM7NBEHVY2N6E --reason ...`
```

**PASS** — task 2 heeds EXACTLY one caution (out-of-scope control stayed out)

## 6. AUTOMATED SETTLEMENT: the system scores itself, against its own convenience

Approving task 2 mechanically meets the premortem's stated falsifier ('a similar task is later approved'). No human resolves anything: settlement.flow retires the caution by the letter of its contract — and task 3 proves the settlement feeds back into retrieval.

```
$ nx approve <txn2>
approved: 1 effects applied and ledgered
auto-settled: premortem 01KWPB5ZKC5PET8684A8D25MQF falsified (its stated falsifier was met by this approval)
```

**PASS** — first automated settlement: premortem falsified by settlement.flow, not a human

**PASS** — the applied artifact still carries the caution that shaped it

**PASS** — the settlement is in the loss ledger with its mechanical trigger

**PASS** — feedback: the settled caution no longer enters working sets

## 7. TRACEABILITY: every conclusion cites its evidence and its reliances

Task 2's judgment names the premortem it relied on. And a note consolidated into a belief cites the exact episode that is its evidence — conclusions trace to evidence, mechanically.

**PASS** — the deliberate derivation cites premortem 01KWPB5ZKC5PET8684A8D25MQF verbatim

```
$ nx consolidate --mock
scanned 1 episodes → 2 candidates → 1 placed, 0 duplicates, 1 rejected
  placed: 01KWPB65FFTZME19ESSQ87HHJH
  rejected: 'everything is always fine': no falsifiers, no bet: a position that cannot say what would kill it may be an episode, never a reliance (specs/record.md §11.1)
```

**PASS** — the consolidated belief's derivation cites episode 01KWPB64YZPEG7JNF29GTW86XB as evidence

## 8. THE HORIZON SWEEPER: stale beliefs expire into the ledger, not into vibes

A belief with a zero-day horizon goes stale; the sweeper (inside consolidation) turns the expiry into a real resolution event. Staleness is bounded even where no falsifier fires.

```
$ nx consolidate --mock  (runs the sweeper first)
swept 1 expired horizon(s) into the ledger
scanned 0 episodes → 0 candidates → 0 placed, 0 duplicates, 0 rejected
```

**PASS** — sweeper ledgered the expiry

**PASS** — the expiry is a resolution event, visible in the loss ledger

## 9. RECEIPTS: signed ledger, tamper-evident record, single-writer lock

Every kernel action is Ed25519-signed. Four flipped bytes anywhere in the log cause hard failure. Restore, and it verifies clean.

**PASS** — every ledger entry signature-verified

**PASS** — 4 flipped bytes → hard failure (BLAKE3 per frame)

**PASS** — restored record verifies clean

---

**Result: 19 passed, 0 failed.**

The question to take away — ask it of any AI assistant, then ask NEXUS:
*"Show me a belief you lost last month, and what killed it."* (`nx bets --lost`)
